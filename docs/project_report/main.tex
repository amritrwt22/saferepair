\documentclass[12pt,a4paper]{report}

% ── Fonts (XeLaTeX) ───────────────────────────────────────────────────────────
\usepackage{fontspec}
\setmainfont{Times New Roman}

% ── Page geometry ─────────────────────────────────────────────────────────────
\usepackage{geometry}
\geometry{a4paper, margin=2.54cm}

% ── Line spacing ──────────────────────────────────────────────────────────────
\usepackage{setspace}
\onehalfspacing

% ── Paragraph style: no indent, space between paragraphs ─────────────────────
\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt}

% ── Core packages ─────────────────────────────────────────────────────────────
\usepackage{graphicx}
\usepackage{caption}
\usepackage{booktabs}
\usepackage{array}
\usepackage{longtable}
\usepackage{tabularx}
\usepackage{listings}
\lstset{
  basicstyle=\ttfamily\fontsize{8}{10}\selectfont,
  language=C,
  breaklines=true,
  frame=single,
  captionpos=b,
  numbers=none,
  xleftmargin=4pt,
  xrightmargin=4pt,
  aboveskip=6pt,
  belowskip=4pt
}
\usepackage{amsmath}
\usepackage{cite}
\usepackage{url}
\usepackage{hyperref}
\hypersetup{colorlinks=false, pdfborder={0 0 0}}

% ── TikZ / PGFPlots (for pipeline diagram and charts) ─────────────────────────
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, patterns}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

% ── Headers / footers ─────────────────────────────────────────────────────────
\usepackage{fancyhdr}
\pagestyle{plain}
\renewcommand{\headrulewidth}{0pt}

% ── Chapter heading style ─────────────────────────────────────────────────────
\usepackage{titlesec}
\titleformat{\chapter}[block]
  {\normalfont\Large\bfseries\centering}
  {}
  {0pt}
  {}
\titlespacing*{\chapter}{0pt}{20pt}{20pt}

% ── TOC depth and heading alignment ──────────────────────────────────────────
\usepackage{tocloft}
\setcounter{tocdepth}{2}

% Center TOC / LoT / LoF titles
% \hspace*{\fill} is used instead of \hfill because \hfill is discarded at line start
\renewcommand{\cfttoctitlefont}{\hspace*{\fill}\Large\bfseries}
\renewcommand{\cftaftertoctitle}{\hspace*{\fill}}
\renewcommand{\cftlottitlefont}{\hspace*{\fill}\Large\bfseries}
\renewcommand{\cftafterlottitle}{\hspace*{\fill}}
\renewcommand{\cftloftitlefont}{\hspace*{\fill}\Large\bfseries}
\renewcommand{\cftafterloftitle}{\hspace*{\fill}}

% Match before/after title spacing to titlesec's \titlespacing*{\chapter}{0pt}{20pt}{20pt}
\setlength{\cftbeforetoctitleskip}{20pt}
\setlength{\cftaftertoctitleskip}{20pt}
\setlength{\cftbeforelottitleskip}{20pt}
\setlength{\cftafterlottitleskip}{20pt}
\setlength{\cftbeforeloftitleskip}{20pt}
\setlength{\cftafterloftitleskip}{20pt}

% ─────────────────────────────────────────────────────────────────────────────
\begin{document}

% ── Front matter (Roman page numbers) ────────────────────────────────────────
\pagenumbering{roman}

\input{chapters/frontpage}
\input{chapters/declaration}
\input{chapters/certificate}

% Uncomment each section as it is completed and approved:
\input{chapters/abstract}
\input{chapters/acknowledgements}
\clearpage
\listoftables
\clearpage
\listoffigures
\clearpage
\input{chapters/abbreviations}
\clearpage
\tableofcontents

% ── Main matter (Arabic page numbers) ────────────────────────────────────────
\pagenumbering{arabic}
\setcounter{page}{1}

\input{chapters/ch1_introduction}
\input{chapters/ch2_literature_review}
\input{chapters/ch3_methodology}
\input{chapters/ch4_results}
\input{chapters/ch5_conclusion}

\input{chapters/references}

% ── Appendices ────────────────────────────────────────────────────────────────
\appendix
\input{chapters/turnitin_similarity}
\input{chapters/turnitin_ai}
\input{chapters/research_paper_page}

\end{document}
